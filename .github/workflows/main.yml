on:
  push:
    branches:
     - master













jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      -name:  'github action'
       run: pwd

       -name:   'test print'
       run: echo Esraa 